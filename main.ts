/**
 * @author Ethan White
 * @version 1.0.0
 * @date 2026-1-7
 * @fileoverview
 */

let questions: string[] = [];
let answers: string[] = [];
let numQ=Number(
  prompt("Welcome to Trivia!\nHow many question would you like to quiz your friend on? Please input the number as a number not as a word")
);
while (numQ >= 1) {
let question=prompt("Enter question :");
let answer = prompt("Enter the correct answer for question :")
numQ=numQ-1
}
